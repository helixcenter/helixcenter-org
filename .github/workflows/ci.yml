name: Deploy

on:
  push:
    branches: [ '**' ]

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7
      - name: Set up Node
        uses: actions/setup-node@v7
        with:
          node-version: '22'
      - run: npm install
      # Make sure tests pass
      - run: npm test
      # Ensure that site can be built successfully
      - run: npm run build

